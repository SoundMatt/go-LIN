# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
dist/
/go-lin
/lintool

# Test output
*.out
*.test
coverage*.html

# Go workspace
go.work
go.work.sum

# Editor
.idea/
.vscode/
*.swp
*.swo

# go-FuSa transient gate reports (regenerated by the CI lifecycle each run).
# Committed evidence (boundary, sci, fmea, safety-case, sbom, provenance, tara)
# plus the source-of-truth inputs (.fusa-reqs/.fusa-hara/.fusa-iec62443, plan
# docs) are tracked; the per-run gate outputs below are not.
check-report.json
cyber-report.json
qualify-report.json
vuln.json
cyber-plan.json
coupling-report.json
coverage-report.json
gofusa-audit-pack.zip
.fusa-evidence.json

# OS
.DS_Store
Thumbs.db
